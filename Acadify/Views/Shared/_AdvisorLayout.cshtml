@using Microsoft.AspNetCore.Http

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@(ViewData["Title"] ?? "Advisor") - Acadify</title>

    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/RahafZh.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/advisor-layout.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/AdvisorLayout.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/AdvisorForm3.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/AdvisorForm4.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/AdvisorChat.css" asp-append-version="true" />

    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap"
          rel="stylesheet" />
</head>

<body>

    <div class="advisor-layout">

        <!-- Sidebar -->
        <aside class="advisor-sidebar">

            <div class="advisor-profile">
                <div class="profile-top">
                    <img src="~/images/user.png" class="advisor-avatar" alt="Advisor" />

                    <button type="button" class="topbar-notif-btn" onclick="toggleNotifications()">
                        <i class="fa-regular fa-bell"></i>
                    </button>
                </div>

                <h4>@(Context.Session.GetString("UserName") ?? "Dr. Amina Gamlo")</h4>
                <p>@(Context.Session.GetString("UserEmail") ?? "agamlo@kau.edu.sa")</p>
            </div>

            @{
                var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
                var isFormsPage =
                    currentAction == "StudentForms" ||
                    currentAction == "StudentFormsByStudent" ||
                    currentAction == "ViewForm" ||
                    currentAction == "PrintForm" ||
                    currentAction == "Form1" ||
                    currentAction == "Form2" ||
                    currentAction == "Form3" ||
                    currentAction == "Form3History" ||
                    currentAction == "Form4" ||
                    currentAction == "Form4History" ||
                    currentAction == "ViewSavedForm3" ||
                    currentAction == "ViewSavedForm4";
            }

            <ul class="advisor-menu">
                <li class="@(currentAction == "AdvisorHome" ? "active" : "")">
                    <a asp-controller="Advisor" asp-action="AdvisorHome">
                        <i class="fa-solid fa-house"></i>
                        <span>Homepage</span>
                    </a>
                </li>

                <li class="@(isFormsPage ? "active" : "")">
                    <a asp-controller="Advisor" asp-action="StudentForms">
                        <i class="fa-solid fa-file-lines"></i>
                        <span>Forms</span>
                    </a>
                </li>

                <li class="@(currentAction == "CommunityAdvisor" ? "active" : "")">
                    <a asp-controller="Advisor" asp-action="CommunityAdvisor">
                        <i class="fa-solid fa-users"></i>
                        <span>Community</span>
                    </a>
                </li>

                <li class="@(currentAction == "Chat" || currentAction == "RequestMeeting" ? "active" : "")">
                    <a asp-controller="Advisor" asp-action="Chat">
                        <i class="fa-solid fa-calendar-check"></i>
                        <span>Meetings</span>
                    </a>
                </li>
            </ul>

            <div class="advisor-logout">
                <form asp-controller="Account" asp-action="Logout" method="post" style="margin:0;">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="logout-link" style="background:none;border:none;">
                        <i class="fa-solid fa-right-from-bracket"></i>
                        <span>Logout</span>
                    </button>
                </form>
            </div>

        </aside>

        <!-- Main Content -->
        <main class="advisor-content">
            @RenderBody()
        </main>

    </div>

    @await Component.InvokeAsync("Notifications")

    <script>
        function toggleNotifications() {
            const overlay = document.getElementById('notifOverlay');
            if (!overlay) return;
            overlay.classList.toggle('show');
        }

        function closeNotifications(e) {
            if (e) e.preventDefault();
            const overlay = document.getElementById('notifOverlay');
            if (!overlay) return;
            overlay.classList.remove('show');
        }
    </script>

    @RenderSection("Scripts", required: false)

</body>
</html>
