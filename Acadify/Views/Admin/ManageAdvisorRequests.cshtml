@model Acadify.Models.AdminPages.ManageRequestsVM

@{
    ViewData["Title"] = "Manage Advisor Requests";
}

@{
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
}

<link rel="stylesheet" href="~/css/Site2.css" />

<div class="page">
    <div class="card">
       

        <table class="table">
            <thead>
                <tr>
                    <th>Student</th>
                    <th>University ID</th>
                    <th>Requested Advisor</th>
                    <th>Email</th>
                    <th>Status</th>
                    <th>Date</th>
                    <th class="actions-col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var r in Model.PendingRequests)
                {
                    <tr>
                        <td>@r.StudentName</td>
                        <td>@r.UniversityId</td>
                        <td>@r.RequestedAdvisorName</td>
                        <td>@r.RequestedAdvisorEmail</td>
                        <td>
                            <span class="badge">@r.Status</span>
                        </td>
                        <td>@r.CreatedAt.ToString("yyyy-MM-dd")</td>
                        <td class="actions-col">
                            <form asp-action="ApproveAdvisorRequest" method="post" class="inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="requestId" value="@r.RequestId" />
                                <button class="btn btn-approve" type="submit">Approve</button>
                            </form>

                            <a class="btn btn-correct" asp-action="CorrectAdvisorRequest" asp-route-requestId="@r.RequestId">
                                Update
                            </a>

                            <form asp-action="RejectAdvisorRequest" method="post" class="inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="requestId" value="@r.RequestId" />
                                <button class="btn btn-reject" type="submit">Reject</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        
    </div>
</div>